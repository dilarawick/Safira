{
  "name": "safira",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "netlify dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "@netlify/functions": "^2.0.0",
    "netlify-cli": "^17.0.0",
    "typescript": "^5.0.0"
  }
}
